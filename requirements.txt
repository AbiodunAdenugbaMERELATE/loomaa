# Development requirements (repo only)
#
# End users should just do:
#   pip install loomaa
#
# Contributors can do (from this folder):
#   pip install -r requirements.txt

-e .

# Tooling
pytest>=7.0.0
build>=1.0.0
twine>=4.0.0